These queries use data.sql file

1. List the event number, date held, customer number, customer name, facility number, and facility name of 2018 events placed by Boulder customers.

A: select eventno,dateheld,Customer.custno,custname,EventRequest.facno,Facility.facname from EventRequest INNER JOIN Customer ON Customer.custno=EventRequest.custno INNER JOIN Facility ON Facility.facno=EventRequest.facno where Customer.city="Boulder" and EventRequest.dateheld BETWEEN '2018-01-01' AND '2019-01-01';

2. List the customer number, customer name, event number, date held, facility number, facility name, and estimated audience cost per person (EstCost / EstAudience) for events held on 2018, in which the estimated cost per person is less than $0.2

A: select Customer.custno,custname,eventno,dateheld,Facility.facno,facname,estcost/estaudience as estcostperaudience from Customer INNER JOIN EventRequest Omer.custno = EventRequest.custno INNER JOIN Facility ON Facility.facno = EventRequest.facno where estcost/estaudience<0.2 and dateheld between '2018-01-01' and '2019-01-01';

3. List the customer number, customer name, and total estimated costs for Approved events. The total amount of events is the sum of the estimated cost for each event. Group the results by customer number and customer name.

A: select Customer.custno,Customer.custname,sum(estcost) as totalcost from Customer INNER JOIN EventRequest ON EventRequest.custno=Customer.custno where status='approved' group by Customer.custno,Customer.custname;

4. Insert yourself as a new row in the Customer table.

A: insert into Customer values('C106','Tarun','ABC 123','Yes','Nikhil',9848477690,'Hyderabad','TG','50006');

5. Increase the rate by 10 percent of nurse resource in the resource table.

A: update Resource set rate=1.1*rate where resname='nurse';

6. Delete the new row added to the Customer table.

A: delete from Customer where custname='Tarun';
